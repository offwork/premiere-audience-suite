import { SharedMaterialModulesModule } from './shared-material-modules.module';

describe('SharedMaterialModulesModule', () => {
  it('should work', () => {
    expect(new SharedMaterialModulesModule()).toBeDefined();
  });
});
